# Product

## Register

product

## Users

소유자 1인 (개발자, IP gate 뒤에 본인만 접근). 데스크탑(터미널/에디터 옆에 띄워두고 수시 활용)과 모바일(이동 중 아이디어 메모) 양쪽에서 동등하게 쓰인다. 구체적 페르소나 한 명을 위한 도구이며, 신규 사용자 온보딩이나 마케팅 표면은 존재하지 않는다.

## Product Purpose

Claude/Codex 같은 AI 코딩 에이전트에게 던질 작업을 모아두는 **개인 프롬프트 디스패쳐**. 흐름은 단순하다.

- `draft` — 떠오른 아이디어를 빠르게 던져두는 인박스
- `ready` — 구체화되어 지금 AI에게 보낼 수 있는 작업
- `done` — AI 작업 완료. 배포 대기 (PR merge 등)
- `deploy` — production 반영됨
- `discarded` — 폐기 (의도적 폐기 + 롤백 모두 포함). 종착지

핵심 액션은 **카드를 ready로 옮기고 → "Copy prompt" → AI 에이전트에 붙여넣기**다. 이 한 동선이 다른 모든 것보다 우선한다. 일반 to-do/팀 칸반이 아니라, AI 워크플로의 큐로 정의된다.

### 전이 규칙 (한 방향)

```
draft ──> ready ──> done ──> deploy
  │         │         │         │
  ▼         ▼         ▼         ▼
        discarded (종착, 복원 불가)
```

- `draft → ready` 만 전진. done/deploy 직행 불가
- 모든 상태에서 `discarded` 가능 (draft 포함, 폐기 기록 남김)
- 역방향 이동 없음. 다시 작업하려면 새 카드 생성
- 카드 자체 hard delete는 별개 액션 (특히 draft 정리용)

## Brand Personality

**겸손함, 쿨함, 개인적.** lazygit, tmux, Linear의 정서가 가깝다. 절제되고 도구로서 자기를 자랑하지 않으며, 매일 같은 자리에 있어주는 무던함이 미덕이다. 화려하지 않지만 만든 사람의 손맛이 느껴지는 결.

목소리 톤: 짧고 기능적. 한국어 마이크로카피 OK ("아이디어 빠르게 등록", "프롬프트 복사 완료"). 격려하거나 축하하지 않는다. 빈 칼럼은 "할 일이 없네요!" 같은 위로 없이 그냥 비워둔다.

## Anti-references

- **Jira, Asana 류 엔터프라이즈 칸반.** 그라디언트 버튼, 아바타, 클러터된 사이드바, 과한 색 다양성. 팀과 회의를 위한 도구의 시각 언어 전체를 거부.
- **AI 생성티가 나는 틀.** glassmorphism, 보라→파랑 그라디언트 배경, gradient text, hero metric 카드, 똑같이 생긴 카드 그리드의 반복.
- **장식적 애니메이션.** bouncy spring, hover에서 카드가 들썩이는 거, 컨페티 파티클, decorative gradient의 모션. 의미 없는 움직임 전부.
- **모노스페이스 ASCII 코스프레.** terminal **정서**는 원하지만, 본문 전체 모노스페이스, ASCII 박스 드로잉, neofetch 스타일 장식, charm.sh 풍 박스 보더는 가짜처럼 느껴짐. 모노는 ID/메타데이터 같은 진짜 코드성 정보에만.

## Design Principles

1. **개인 도구 우선, 소비자 제품 아님.** 1인의 매일 반복 동선을 최적화한다. 첫인상, 전환율, 온보딩 같은 마케팅 가치는 무시. 100번째 방문에서 가장 좋아야 한다.
2. **프롬프트 디스패치가 척추.** ready로 옮기고 "Copy prompt"하는 한 동선이 항상 가장 빠르고 명확해야 한다. 다른 기능(편집, 삭제, 이동)은 이 동선을 방해하지 않는 한도 안에서만 존재.
3. **조용한 자신감, 코스프레 아님.** lazygit/Linear의 절제를 따르되, 풀 모노스페이스 터미널 흉내는 안 낸다. 캐릭터는 타이포 위계와 색의 절제로 만들고, ASCII 장식이나 boxy frame으로 만들지 않는다.
4. **모바일과 데스크탑 모두 1급 시민.** 모바일은 아이디어 투입(draft 추가, 빠른 메모), 데스크탑은 처리(ready 만들기, prompt 복사). 두 모드 모두 의도적으로 설계되며, 한쪽이 "축소판"으로 느껴지면 안 된다.
5. **빈 것을 채우지 않는다.** 1장짜리 컬럼은 1장으로 보여준다. 빈 상태 일러스트, 동기부여 카피, 진행률 차트 같은 가짜 풍성함을 만들지 않는다. 정직한 빈 공간이 기본값.

## Accessibility & Inclusion

단일 사용자 도구이므로 WCAG 준수를 목표로 삼지 않는다. 다만 소유자 본인의 일상 사용을 해치지 않는 선에서 다음은 유지한다.

- 다크 환경에서의 기본 가독성 (텍스트/배경 충분한 명도 차)
- focus ring 유지 (키보드로 빠르게 다닐 때 필요)
- Esc로 모달 닫기, Cmd/Ctrl-S로 저장 같은 표준 단축키
- prefers-reduced-motion 존중 (사용자 본인이 필요할 때 켤 수 있도록)

스크린리더 최적화, 색맹 고려, 다국어 등은 명시적 비-목표.
