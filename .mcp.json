{
  "mcpServers": {
    "kanban-ready": {
      "command": "npx",
      "args": ["tsx", "mcp/server.ts"],
      "env": { "KANBAN_API_URL": "https://kanban-ready.jws71010452.workers.dev" }
    },
    "kanban-ready-local": {
      "command": "npx",
      "args": ["tsx", "mcp/server.ts"],
      "env": { "KANBAN_API_URL": "http://localhost:8787" }
    }
  }
}
